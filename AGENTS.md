# AGENTS.md — 自动化编码代理执行契约

> 本文对**自动化驱动的编码代理**（Codex / tcd 派发的会话等）具有最高优先级，高于任何通用技能约定。
> 人类协作者请读 `README.md` 与 `docs/DESIGN.md`。

## 你的运行环境

- **对端是自动化驱动，不是人**。没有人在旁边点「批准」。
- **技能/工具的审批门槛视为已预先批准**，不要停下来等确认。
- **不要为了等自己 spawn 的子代理而结束回合**。交付物清单没做完就继续做。
- 完成 = **交付物清单全部落盘 + 验收命令全绿**，不是「我觉得做完了」。

## 这个项目是什么

Vibe Workbench：把 AI 的思考渲染成可交互网页，用户在网页上选择/批注/改写，提交后唤醒 AI 续跑。

- **零运行时依赖**。`package.json` 没有 dependencies，也不要加。Node 内置模块 + 原生 DOM API。
- 前端不用任何框架、不打包、不转译 —— 浏览器直接 `import` ESM。
- 测试用 Node 内置 `node:test`，不引第三方测试框架。

## 硬性约束

### 1. 视觉数值只能写在 `src/render/theme.css`

颜色、字号、行高、字距、间距、圆角、版心宽度 —— 全部住在 `theme.css` 的 `:root` 里，
`app.css` 只做布局与组件结构，数值一律 `var()` 引用。

- ❌ `font-size: 14px`（app.css 里出现就会被测试拦下）
- ✅ `font-size: var(--fs-md)`
- 需要新档位就去 `theme.css` 加，别在 app.css 里写死。
- 任何引用 `app.css` 的 HTML 必须**同时引 `theme.css` 且排在前面**，否则令牌全部落空。

### 2. 固定亮色，不许跟随系统配色

不得出现 `@media (prefers-color-scheme: ...)`。创始人明示「黑底看着不舒服，都默认白底」。
`:root` 上有 `color-scheme: light only`，别删。

### 3. 中文排版四条底线

1. 字体栈必须显式点名中文字体（`PingFang SC` 等）。只写 `-apple-system, system-ui` 会让中文兜底到又宽又扁的 Heiti SC。
2. 正文行高 ≥ 1.75（中文方块字在 1.5 下挤成一堵墙）。
3. 字重封顶 600。PingFang SC 最粗只到 Semibold(600)，写 700/800 时浏览器不会为中文合成假粗体，
   实测 600 与 700 的字形包围盒完全一致 —— 加粗是白写的，层级反被压平。
4. 灰度抗锯齿只在 `@media (min-resolution: 2dppx)` 下开。1 倍屏上它会把中文笔画削细削淡，观感发「扁」。

### 4. 改了 `src/render/` 或 `src/server/` 要考虑「用户拿到的是哪份」

- `server.mjs` 是常驻进程，**不会热加载**，改完必须重启才生效。
- 新增的前端静态资源若参与缓存击穿，要加进 `server.mjs` 的 `ASSET_VERSION_FILES`，
  否则改了它页面不会自动刷新。

### 5. 不做过度设计

按需求实现。不要为「将来可能需要」加抽象层、配置项、插件点。
解耦 ≠ 加功能：把纠缠的东西拆开是对的，顺手加一堆没人要的能力是错的。

## 工作纪律

### 文件域隔离

派工提示词会划定你能碰的目录。**只改域内文件**，域外的一个字都不要动。
多个任务包可能在同一工作区并行跑。

### 禁止 git 操作

除非提示词明确要求，**不要执行任何 git 命令**（add / commit / checkout / stash / branch 都不行）。
提交由驱动方统一做。

### 测试

- 每个新模块都要有对应的 `tests/unit/<名字>.test.mjs`。
- **纯逻辑必须能脱离浏览器跑**：不依赖 `document` / `window` / `localStorage`，
  需要这些的话把它们作为参数注入。
- 全量：`npm test`。交付前必须全绿，并在总结里贴出 `pass N / fail 0` 的统计。
- **不许为了让测试过而改断言或删测试**。测试红了说明代码有问题，去修代码。
- 现有测试有一类是「读源码字符串做正则匹配」（如 `assert.match(renderApp, /…/)`）。
  这是历史包袱，说明那部分代码不可测。**你重构后应该把它换成真正的行为测试**，
  但不要在没有替代品的情况下直接删掉。

### 重构的安全网

这是**纯重构**：外部行为一个字节都不能变。

- 先跑一遍 `npm test` 记下基线数字，改完必须 ≥ 基线且 fail 0。
- 搬函数时**原样搬**，不要「顺手优化」逻辑。要改逻辑，单独说明。
- 每搬完一块就跑一次测试，别攒到最后。
- 中文注释保留原文搬过去，别重写、别翻译。

## 交付时说什么

1. 交付物清单逐项对照（文件路径 + 是否完成）
2. `npm test` 的 `pass/fail` 统计原文
3. 改动摘要：搬了什么、拆成了什么、有没有行为变化（应该没有）
4. 没做完的部分要明说，不要含糊过去
